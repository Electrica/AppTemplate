define('apptemplate', ['jquery', 'bootstrap'], function ($) {
    'use strict';

    var AppTemplate = {
        init: function () {
            console.log('Loaded "apptemplate"');
        }
    };
    AppTemplate.init();

    return AppTemplate;
});